import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  dayContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'stretch',
  },
  week: {
    margin: 0,
    padding: 0,
    flexDirection: 'row',
    width: '100%',
  },
});
